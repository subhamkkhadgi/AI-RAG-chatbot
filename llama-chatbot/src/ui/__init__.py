"""Streamlit presentation layer for the chat application.

This package contains the UI components that handle user interaction
and display.  It depends on ``ChatService``, models, and config, but
never on provider SDKs directly.
"""
